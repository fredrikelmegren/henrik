using UnityEngine;
using System.Collections;

/// <summary>
/// Currently only used to determine the type of unit, but in a real game would probably have much more logic related to the actual type.
/// </summary>
public class DemoEnemy : MonoBehaviour
{
    void Start()
    {
    }
}
